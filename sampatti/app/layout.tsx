import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Sampatti - Investment Management Platform',
  description: 'Manage all your investments in one secure place. Track assets, add nominees, store documents, and more.',
  keywords: 'investment, finance, portfolio management, nominee, assets, tracking',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}